FROM alpine:3.20

ARG APP_NAME=warehouse-mobile
ENV APP_NAME=${APP_NAME}

CMD ["sh", "-c", "echo \"${APP_NAME} placeholder container. Replace this Dockerfile with real service/app build steps.\" && sleep infinity"]
